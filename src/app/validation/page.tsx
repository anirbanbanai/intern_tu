import Validation from '@/components/Validation/Validation';
import React from 'react';

const page = () => {
    return (
        <div>
            <Validation/>
        </div>
    );
};

export default page;